/**
 * Shared constants for social-sharing (Open Graph / Twitter Card) meta tags.
 * Kept in one place so every page builds absolute URLs consistently — GitHub
 * Pages serves this app from the `/horizon/` base path (see vite.config.ts).
 */
export const SITE_URL = 'https://ahmednassar7.github.io/horizon'
export const OG_IMAGE_URL = `${SITE_URL}/og-image.png`
